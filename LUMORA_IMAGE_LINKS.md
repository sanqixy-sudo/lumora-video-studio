# 图片链接读取确认 · v7

每条直接填写的参考图片链接旁增加“我确认图片可用，跳过读取检查”。默认不勾选；仅对当前链接生效，修改链接或切换模型后取消确认，删除链接时移除对应确认。不保存到浏览器或数据库。

勾选后跳过本机远程读取和图片尺寸预检，以原图片 URL 提交任务；未知尺寸使用已有可空字段，不伪造尺寸。生成渠道仍需要能读取图片，不能保证上游生成一定成功。未勾选仍按原规则读取和校验；预设权限、素材数量、URL 协议格式、VEO 视频编辑下线、额度和任务权限规则不变。

后端四个单条/批量、JSON/表单生成入口均接受可选 `confirmed_reference_image_urls` 列表。该列表只匹配实际提交的直接图片 URL，不能绕过预设权限，不改变上游协议，也没有数据库迁移。

通用图片检测错误区分 HTTP 状态码、超时、连接失败和图片解析失败，避免将所有网络异常混称为“不是有效图片”。

## 用户反馈链接实测

链接：`https://cdn.nodeimage.com/i/sSaBKV5f4cGXZPXLvlLsXs9PjWbAYlib.png`

2026-09-08，本机网络测试：项目原有探测函数成功读取 720×1280、image/png；另一次完整 GET 返回 HTTP 200，284,034 字节，Pillow PNG 完整性校验通过。该结果证明当前本机可读取，不能证明旧服务器当时或上游服务始终可读取。未取得旧服务器当时日志，不能确定截图报错的历史原因。原始记录：`runtime/image_confirmation/reported-url.json`。

## 验收

- 69 项单元测试通过，包含新增 6 项：逐链接确认、默认校验、协议/数量/下线限制、预设权限、四个 HTTP 入口以及网络错误分类。
- 4 个专项浏览器视图通过：390/1440px × 深浅主题，覆盖默认不发送确认、勾选后发送、失败保留输入、改 URL 清除确认、多链接独立状态、渠道切换清除、标准图片读取失败后的确认及取消确认后恢复检查。axe 无违规、页面无横向溢出。
- 既有手机关键交互 7 组重新执行通过，包含生成、导入、登录注册、后台保存失败和焦点返回。
- 浏览器与生成 API 测试使用模拟接口，不发起真实付费生成；真实外部访问仅用于读取用户提供的图片。
- 运行 `python -B -m unittest discover -s tests`、`node scripts/check_image_confirmation.cjs`；浏览器脚本需要本地 8100 模拟预览服务及 Playwright。

本轮更新包为 Docker 源码更新 ZIP，未构建镜像或部署服务器；v6 及更早包保留。全站手机矩阵沿用 v6 记录，本轮只重测受影响功能。
