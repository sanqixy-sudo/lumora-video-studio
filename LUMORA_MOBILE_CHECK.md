# Lumora 手机端专项验收 · 20260908-lumora6

本地模拟环境下，全部 31 个现有页面完成手机端复查；发现的问题已修复。生产页面模板与预览页面逐项核对，包含登录/注册、生成、任务及详情、批次、作品、广场、额度、素材、引导、所有管理后台和无权限页。

## 环境与覆盖

- 真实 Jinja 模板及公共 CSS/JS，本地模拟数据和接口，Chromium 触屏模式；未发起真实付费生成或部署服务器。
- 浅色、深色 × 320×740、375×812、390×844、430×932、768×1024、844×390，包含手机横屏。
- 372 个基础页面组合，加页尾和导航、账户、抽屉、菜单、用量编辑、注册及设置标签展开状态，共 1,038 项记录。
- 每项检查页面边界、实际主题、输入字号、可见点击区域及弹层宽度；375px 两主题的 62 个基础页另执行 axe 规范检查。
- 宽表格保留自身横向滚动，不挤压全部列进入手机屏幕。

## 修复记录

- `app/static/workbench/mobile.css:17`：固定宽度的用量编辑卡片撑开隐式网格列，导致 375px 屏幕上弹层宽 480px。模态容器改为可收缩网格列，卡片不再越过屏幕。
- `app/static/workbench/mobile.css:3`：统一触屏按钮、菜单、主题图标、导航及媒体操作至少 44px；独立返回/批次/额度链接扩大点击区域。
- `app/static/workbench/mobile.css:5`：触屏输入及选择框至少 16px，包含生成页及后台用户筛选；不会因手机横屏而恢复成桌面小字号。
- `app/static/workbench/mobile.css:8`：展开素材链接、日志等 summary 提供至少 44px 高度。
- `app/templates/workbench/base.html`、`workbench/login.html`、`errors/403.html`：统一加载手机样式；包含普通用户、子管理员及总管理员共享组件。

## 复查结果

- 1,038 项布局/状态记录：页面溢出 0、弹层超宽 0、无提示裁切 0、可见有效点击区域不足 44px 的记录 0、表单输入字号不足 16px 的记录 0。
- 62 个基础页 axe 检查无违规；页面脚本错误 0，本地请求 HTTP 错误 0。
- 手机关键交互 7 组通过：新建/编辑抽屉及失败恢复；设置分组完整保存；用户删除校验和返回筛选；作品选择/预览；宽表格菜单及移动导航；空态/筛选无结果/权限页；素材选择、提示词导入、101 条拒绝、替换确认、生成失败输入保留、渠道切换、登录注册失败及成功登录。
- 63 项单元测试通过，包含子管理员 HTML/JSON/XLSX 数据权限及写入 403 测试。
- 先前的子管理员专项（6 个视图）结论沿用；本轮共享样式覆盖子管理员，未把总管理员预览声称为全部真实账号登录测试。

## 证据与复现

- 修复前：`runtime/mobile_review/report.json` 与原截图；中间修复：`runtime/mobile_review_fixed/`；最终：`runtime/mobile_review_final/report.json`、`interactions.json` 及逐页截图。
- `node scripts/check_lumora_mobile.cjs` 执行完整矩阵；可用 `MOBILE_OUT` 指定新证据目录，保留原证据。
- `node scripts/check_lumora_mobile_flows.cjs` 执行关键手机操作；需要本地 8100 模拟预览服务及已有 Playwright。
- 测试工具的全页截图会重置 Chromium 的触屏模拟，最终脚本在每次测量前恢复并断言粗指针状态；早期截图后的横屏小控件记录部分属于测试环境偏差，不作为产品缺陷累计。
- 宽表格菜单测试先等待自动滚动完成，避免测试滚动事件与菜单按设计关闭产生竞态。

## 尚未覆盖

没有实体 iPhone/Android、移动 Safari/Firefox 或微信内置浏览器测试。软键盘弹起、浏览器地址栏动态收缩、真实刘海安全区、文件选择器/系统下载器和真实网络环境尚未实测；浏览器尺寸模拟不等同于这些真机检查。正常动效沿用此前专项结果，本轮矩阵使用减少动态效果保证测量稳定；未新增性能提速结论或生产端到端通过声明。
