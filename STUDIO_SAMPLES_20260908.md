# 流光 Lumora 全站重设计：第一阶段样板评审

更新日期：2026-09-08（配色与登录构图第二稿）

本阶段完成电影感登录、生成工作台、任务详情和后台用户四类可操作样板。使用现有 FastAPI/Jinja2、原生 JavaScript 和 CSS，页面直接采用最终实施用的模板与公共组件。

依据已批准方案「用户确认设计方向后，将组件推广至全部页面」，当前交付用于确认设计方向。其余页面、全站旧 CSS 清理、统一发布资源版本和新 Docker 更新包属于下一阶段。本阶段不部署服务器，不调用付费生成接口。

## 评审入口

- 综合评审：<http://127.0.0.1:8100/design>
- 登录与注册：<http://127.0.0.1:8100/login>
- 生成工作台：<http://127.0.0.1:8100/app>
- 已完成详情：<http://127.0.0.1:8100/app/jobs/101/page?state=completed>
- 处理中详情：<http://127.0.0.1:8100/app/jobs/101/page>
- 失败详情：<http://127.0.0.1:8100/app/jobs/101/page?state=failed>
- 后台用户：<http://127.0.0.1:8100/admin/users/page>

综合评审支持深浅主题、模拟请求成功/失败、新旧并列和独立打开。并列模式中每个窗口宽度约为屏幕一半；评审桌面同屏工作台时请使用「只看新版」或「独立打开」。

预览仅绑定本机，使用虚构数据，不读取或修改业务数据库。登录、注册、批量提交、新建用户及任务操作返回模拟结果；不会持久保存新用户或任务。链接到四类样板之外的页面时，仍可能展示上一版页面。此预览服务不用于生产。

## 本阶段实现

- 独立的主题、公共组件、页面和动效样式；四类样板不加载旧 `app.css`。
- 中性黑灰深色、白灰浅色、少量冷蓝强调色；首屏主题应用、浏览器偏好及跟随系统。
- 桌面导航展开/收起、手机导航抽屉、账户菜单、主题选择、原生确认框、焦点循环和关闭后返回。
- 登录页删除绿色风景插画、圆球与倾斜卡片，采用深色品牌区、两行标题、低对比度 LUMORA 字样和本地 SVG 光线；深色品牌区使用冷光，浅色品牌区独立使用雾白渐变、深灰文字和银蓝柔光，品牌区与表单同步切换主题。构图首次访问一次性进入；登录/注册切换；错误保留非敏感输入。
- 桌面生成参数栏、独立滚动的素材与提示词、底部提交栏、默认收起的最近任务。
- 素材抽屉「选择后应用」，取消不更改原素材；缩略图、计数及渠道能力联动。
- 提示词新增/复制/删除/导入；追加默认、替换确认、100 条边界、额度不足和提交失败恢复。
- 任务详情视频与操作分栏；字段标签在上、内容在下；远端编号独占行并支持复制；竖屏视频完整显示。
- 后台用户列表、筛选、新建抽屉及移出表格滚动容器的操作菜单。现有用户详情编辑页将在后续推广时统一。
- 共用既有接口地址、表单字段、业务提交及轮询逻辑；Pod/VEO 视频编辑下线规则、Wuyin 能力和历史兼容测试继续通过。

## 本地启动

在项目目录使用现有虚拟环境：

```powershell
.\.venv\Scripts\python.exe -B scripts/preview_studio.py --serve
```

默认地址为 `127.0.0.1:8100`。当前评审服务已启动；端口占用时无需重复启动。模板改动后可运行下列命令重新生成样板 HTML，静态资源直接从工作目录读取：

```powershell
.\.venv\Scripts\python.exe -B scripts/preview_studio.py
```

本地可播放视频由 `scripts/create_studio_preview_clip.cjs` 使用 Canvas/MediaRecorder 生成，不是上游生成结果。上一版模板、静态资源和渲染页面保存在 `runtime/before-studio-20260908/`。

## 验收结果

| 检查 | 结果与范围 |
| --- | --- |
| 现有单元测试 | `python -B -m unittest discover -s tests -v`：54 项通过 |
| 四类样板布局 | 5 个入口 × 6 种宽度 × 2 套主题 = 60 组通过，无页面级横向溢出及 JavaScript 页面错误 |
| 宽度 | 375、390、768、1024、1440、1920px |
| 详情补充状态 | 完成/失败 × 6 种宽度 × 2 套主题 = 24 组通过；包含可播放视频、本地下载入口和长字段宽度检查 |
| 放大/横屏 | 4 页 CSS 200% 放大压力检查、844×390 登录横屏检查通过；与上一行合计 29 项扩展检查 |
| 提示词/素材 | 取消与应用、追加、替换取消/确认、100 条及第 101 条拒绝、额度不足、Wuyin/VEO 输入切换通过 |
| 失败恢复 | 登录、注册、生成、后台新建用户模拟失败时非敏感输入保留通过 |
| 焦点/菜单 | 原生模态 Tab 循环、Escape 关闭及焦点返回、表格菜单移出滚动容器、手机导航通过 |
| 轮询 | 详情更新保留页面时间基准及滚动位置；未整页刷新 |
| 动效 | 正常动效和减少动态效果分别运行；快速打开/关闭抽屉、最终状态与进入完成检查通过 |
| 主题 | 浅色、深色、系统主题变化通过 |
| 点击区域 | 手机导航、素材选择、新增提示词、提交四个主要入口至少 44×44px |
| 对比度 | 40 组正文/次级文字/状态/按钮主题色配对至少 4.88:1；这不是全部实际元素的对比度审计 |
| 资源/语法 | 扩展检查无资源请求失败；4 个共用 JavaScript 文件语法通过 |
| 评审入口 | 新版及上一版登录/生成 iframe 均可加载 |

浏览器脚本：`scripts/check_studio_samples.cjs`、`scripts/check_studio_quality.cjs`、`scripts/check_studio_delivery.cjs`。脚本使用本机 Playwright；可通过 `SORA_PLAYWRIGHT` 指定其他安装路径。

机器报告位于 `runtime/studio_review/`：`report.json`、`layout.json`、`quality-report.json`、`delivery-report.json`。

## 截图与动效

文件均位于 `runtime/studio_review/`：

- `creator-filled-dark-1440.png` / `creator-filled-light-1440.png`：有素材和提示词的工作台。
- `_login-dark-1440.png` / `_login-light-1440.png`：登录双主题。
- `detail-completed-dark-1440.png` / `detail-completed-light-1440.png`：可播放视频详情。
- `_admin_users_page-dark-1440.png` / `_admin_users_page-light-1440.png`：后台用户列表。
- `materials-drawer-light-1440.png`：素材选择抽屉。
- 对应 `390.png` 文件：手机布局。
- `motion/page@901d7881d13d2b9be60ee7ad0ea41a01.webm`：本轮正常动效录屏，包含登录/注册切换与抽屉快速开关。
- `review-montage.jpg`：四类样板视觉审查拼图。

## 尚未执行的最终验收

以下不计为通过：物理手机/平板及软键盘、浏览器真正的 200% 缩放控件、跨浏览器行为、生产数据库/权限环境、实际付费上游、Docker 构建与更新回退演练、全站逐元素对比度审计、隐藏页面/退避恢复/重复提交等完整业务矩阵。本地检查不表示真实服务器性能提升。

后续确认方向后，推广到任务/批次列表、作品与广场、额度、全部后台及错误/空状态；完成上述回归，再产出 Docker 更新 ZIP、校验文件和更新回退说明。上一版交付 `release/sora-console-docker-update-20260907.zip` 保留，不覆盖。

## 第二稿调整范围

依据用户对灰绿配色和登录页图案的反馈，重做四类样板的主题色与登录品牌构图；功能和已批准的分阶段评审边界保持原样。登录专属样式已从公共页面文件拆入 `app/static/workbench/login.css`，旧图案样式移除。已重新通过 60 组布局、29 项扩展检查和 40 组主题文字色检查。单元测试沿用本阶段已有的 54 项通过记录，本次纯视觉调整未重复运行。

修复本地预览服务对所有 SVG 返回素材占位图的问题，本地静态 SVG 现在按实际文件展示。上一稿源文件保存在 `runtime/before-neutral-review-20260908/`。

## 登录浅色品牌区修订

依据用户反馈，保留已认可的深色登录设计，新增独立浅色品牌区。浅色用雾白到白色的渐变衔接表单，深灰标题和银蓝光影；使用单独的本地 SVG，避免直接反相暗色图片。全站隐藏滚动条的偏好继续保留，滚动行为不变。

## 轻动效补充

依据用户对侧栏和最近任务的反馈，增加以下可打断的短过渡：

- 侧栏收放：220ms，背景边缘与主区位置同步过渡；主表格仅结算一次宽度，不逐帧拉伸。首次载入已保存的导航偏好不播放。
- 最近任务、素材链接：展开 220ms、收起 180ms，箭头同步转向。使用原生 details，支持键盘操作；不支持 interpolate-size 的旧浏览器保留即时原生折叠。
- 账户菜单：立即出现，100ms 淡出。
- 导入面板：140ms 进入、100ms 退出，关闭时立即禁止隐藏字段接收焦点，并把焦点返回入口，保留输入。
- 素材选中：120ms 背景与边框反馈。提示词新增继续使用现有 140ms 轻移淡入。

系统开启减少动态效果时关闭这些过渡；不加入业务页背景循环、数字翻滚和滚动入场。检查包括中间帧、快速反向、键盘、输入保留和减少动态效果，结果见 `runtime/studio_review/micro-motion-report.json`；演示录屏位于 `runtime/studio_review/micro-motion/`。

## 品牌更新

当前产品名称统一为流光 Lumora。Logo 按用户提供的原始 SVG 路径裁去透明留白，保留比例与三色配色；登录、侧栏、页面标题、错误页、浏览器图标和评审页同步替换。详见 `BRAND.md`。旧快照和历史发布包继续保留旧名称。
