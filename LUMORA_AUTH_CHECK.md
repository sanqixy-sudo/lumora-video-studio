# Lumora v9.1 认证与最终补测

日期：2026-09-08。当前交付：20260908-lumora9-1。本版本包含 v9 雾蓝紫灰配色和以下追加修复；v8、v9 原包均保留。

## 服务器排查结果

按用户授权通过 SSH 和隔离浏览器排查原服务器。运行版本为 v7，账号 chenlin 的数据库状态为 active，角色 user。使用用户提供的凭据请求登录接口返回 HTTP 200，进入 /app；退出接口返回 303 /login 并清除 Cookie。随后使用实际浏览器点击登录、账户菜单、退出，正常返回登录页，Cookie 数为 0，没有“操作已保存”提示或脚本异常。

因此本次没有复现用户截图中的服务器故障，不能把它归因于禁用账号。近期认证日志存在一次 401 后跳转到嵌套 login next 地址，证实旧前端会把登录失败错误归入会话过期；没有足够证据认定该日志属于截图那次请求。未部署服务器、未修改账号状态、密码、权限或业务配置。认证测试按现有逻辑更新最后登录时间。密码、会话令牌不保存在报告或交付文件中。

## 已确认并修复的本地问题

- 旧 fetchJSON 将所有 403 固定改写为“没有执行此操作的权限”，丢失真实原因。现在保留服务端 JSON 错误；无 JSON 的 403 显示明确的登录请求拒绝提示，不推断账号被禁用。
- 登录接口的 401 表示凭据错误，旧前端却作为会话过期跳转。现在在表单旁显示“用户名或密码错误”，保留账号输入，不产生嵌套 next；受保护业务接口的 401 仍按原规则跳转登录。
- 密码作为原始值提交，不再被通用序列化去除首尾空格。
- 顶栏和 403 页的退出表单改为原生 POST，由现有 303 响应导航，不经过通用“保存”反馈。原接口和 Cookie 删除规则未变。本地预览退出响应同步改为生产使用的 303。
- 重新提交登录时隐藏旧过期提示；过期提示改为中文。
- v9 中一帧滚动修正仍受兄弟提示词延迟渲染影响。追加 :focus-within 时解析全部提示词卡片高度，保留离开编辑后的延迟渲染。桌面/手机 8 次焦点跳转、各连续 10 帧均无遮挡。

## 实际验证

| 检查 | 结果 |
|---|---|
| 全部单元测试 | 73 项通过，新增实际认证路由的正常/禁用/密码错误、角色目标、登录 Cookie 及退出后 401 检查 |
| 认证浏览器专项 | 12 项通过，390/1440px，JSON 401/403/503、HTML 403、密码原样提交、错误保留、顶栏和 403 页原生退出 |
| 原有规范关键交互 | 6 组复查通过，保存失败保留、错误焦点、手机抽屉、100 条导入、最近任务恢复、键盘预览 |
| 焦点稳定性 | 8 次跳转 × 10 帧通过，首项/中间项/末项；runtime/auth_review/prompt-focus-stability.json |
| 服务器浏览器观察 | v7 当前正常，runtime/auth_review/server-probe.json；此证据不是新版本部署验证 |

配色的 31 页双主题、534 个规范状态、1,038 项手机矩阵和对比度详见 LUMORA_PALETTE_CHECK.md，均属于本轮 v9 配色验收；v9.1 针对认证和提示词焦点执行以上补测。真实设备与服务器新版本尚未验证，不计为通过。

## 更新

最终源码更新包：release/lumora-docker-update-20260908-v9.1.zip。视觉与报告包：release/lumora-visual-review-20260908-v9.1.zip。附同名 .sha256 文件，保留 v8、v9 回退包。使用 LUMORA_DOCKER_UPDATE.md 的原项目更新方式；本轮不直接部署服务器。
